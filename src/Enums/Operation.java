package Enums;

/**
 * Enum to provide STE with an indication of
 * what type of encryption it's dealing with.
 * 
 * @author sam
 */
public enum Operation {
	Symmetric, Asymmetric, Password;
}
